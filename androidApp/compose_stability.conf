// Classes from :analysis, which is compiled without the Compose compiler and
// so has every type inferred unstable. PoseMetrics is an immutable holder of
// nullable Doubles: telling the compiler so lets MetricsStrip skip instead of
// recomposing behind the once-per-display-frame position poll.
com.badmintontracker.analysis.player.PoseMetrics
